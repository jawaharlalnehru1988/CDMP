import { SeparateTextPipe } from './separate-text.pipe';

describe('SeparateTextPipe', () => {
  it('create an instance', () => {
    const pipe = new SeparateTextPipe();
    expect(pipe).toBeTruthy();
  });
});
